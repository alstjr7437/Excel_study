	함수 할때 고정할시 f4 누르기 

>= 는 이상 <= 는 이하
>는 초과 <는 미만

가로 균등 분할 들여쓰기 맞춤
맞춤 > 셀서식 > 가로 > 균등분할(들여쓰기)

제일 굵은선 할때는 다른 테두리 들어가서 하기

사용자 지정 표시형식
표시형식 > 사용자 지정 > 
#,##0 > 3,000
[조건] true; false 조건이 true일시 앞 false일시 뒤
*? 빈열 폭 만큼 ? 가 들어감
**를 하면 숫자 앞에 빈칸은 전부 *

수식 이용 안하여 몇이상은 굵게 이런식일시
조건부 서식 > 새서식 규칙 > 다음을 포함하는 셀만 서식지정
셀 값 해당 범위 > < 이런것 숫자
예) 셀 값 > 365  365초과일시 서식 실행

SUM > 다 더한값
AVERAGE > 평균
MAX > 범위 안 최대값
MIN > 범위 안 최소값

AND(?,!) > ?와 !둘 다 true일시 true
OR(?,!) > ?와 !둘 중 하나만 true일시 true

ROUND(수, ?) ?짜리 까지 수를 반올림
ROUNDUP(수, ?) ?짜리 까지 수를 올림
ROUNDDOWN(수, ?) ?짜리 까지 수를 내림

COUNT 범위에서 숫자가 포함된 셀의 수 구하기
COUNTA 범위에서 비어 있지 않은 셀의 수 구하기
COUNTBLANK 범위에서 비어있는 셀의 수 구하기

LEN() 괄호안에 문자 개수 구하기

CHOOSE(넘버,1,2,3) 넘버가 1이면 1 실행 2면 2실행

FIND("?",A4) > A4에 ?가 
있으면 > 몇번째에 있는지 출력
없으면 > false

ISERR() > ()안에 오류값을 참으로 만들어줌. 

RANK.EQ(?,1:5) > ?가 1~5에서 몇번째로 많은지 내림차순으로 
마지막에 0아닌 수 입력시 오름차순

IF(?, 1, 2) ?가 
true > 1 수행	false > 2 수행

차지율 구할때
B4/SUM($B$4:$B$19)*100

평균과 차
E4-AVERAGE(B4:E4)
AVERAGE(B4:E4)-E4

-------------------------------------------------------
필터
제목도 가져오기
데이터 > 필터 (숫자면 숫자 필터)
하위는 없음(상위로 들어가서 하위 바꾸고 하기)

차트 
삽입 > 차트 만들기 차트이동 등
차트 레이아웃 
제목 수정시 +버튼 누르고 제목들어가기

피버테이블
삽입 > 피버테이블 제목까지 전부 선택
값에 옮기고 값필드 평균으로
값 생긴거 행으로

부분합
데이터 > 정렬 > 원하는대로 하기 
정렬하고 부분합 눌러서 설정하기

데이터 > 정렬
대분류 오름차순
부분합 > 대분류
최대 셀 정하기

텍스트나누기
데이터 > 텍스트 나누기
너비 일정함 > 열가져오기 않기 등 조심하기
안가져올시 선택 후 열가지않음 

목표값과 시나리오
데이터 > 가상분석 > 
목표값 > 수식셀 찾는값 셀 넣고 바꾸기
목표할곳 선택 목표값 선택 다른곳 선택

데이터 > 가상분석 > 
시나리오 관리자 > 추가 > 시나리오 이름 넣고 변경 셀 넣고 값 넣기 
요약 등





